[project]
name = "examen2bim-webapp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic",
    "fastembed==0.8.0",
    "numpy",
    "openai",
]

[tool.vercel]
entrypoint = "app:app"
